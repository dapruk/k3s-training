apiVersion: v1
kind: Service
metadata:
  name: training-fe-only
  namespace: k3s-training
spec:
  type: NodePort
  selector:
    app: training-fe-only
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081
